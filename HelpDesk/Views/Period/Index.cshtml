 @{
    ViewData["Title"] = "Period List";
    Layout = "~/Views/Shared/_Layout1.cshtml";
}

@model IEnumerable<Period>

<div class="card card-primary">
    <div class="card-header">
        <h3 class="card-title">PERIOD LIST</h3>

    </div>

<section class="content-header">
    <h1>
    Period
    </h1>
        <ol class="breadcrumb float-sm-right">
            <li><a asp-controller="dashboard" asp-action="index"><i class="fa fa-dashboard"></i>Home</a></li>
            &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
            <li><a href="#">Period List</a></li>
        </ol>
</section>

 <!-- form start -->

 <div class="row">
                        <div class="col-12">

            @if (ViewBag.msg != null)
            {
                if (ViewBag.msg == "Done")
                {
                    <div class="callout callout-success">
                        <h4>Successful</h4>
                    </div>
                }
                else
                {
                    <div class="callout callout-danger">
                        <h4>Failed</h4>
                    </div>
                }

            }
                            <div class="card">
                                <div class="card-header">

                                    <div class="card-tools">
                                        <div class="input-group input-group-sm" style="width: 150px;">
                                            <input type="text" name="table_search" class="form-control float-right" placeholder="Search">

                                            <div class="input-group-append">
                                                <button type="submit" class="btn btn-default">
                                                    <i class="fas fa-search"></i>
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="card-body table-responsive p-0">
                                    <table class="table table-hover text-nowrap">
                                        <thead>
                                            <tr>
                                                <th>Id</th>
                                                <th>Name</th>
                                                <th>Color</th>
                                                <th>Status</th>
                                                <th>Action</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @foreach (var period in Model)
                                            {
                                               <tr>
                                                 <td>@period.Id</td>
                                                 <td>@period.Name</td>
                                    <td>
                                        <span class="badge bg-@period.Color">
                                            &nbsp;&nbsp,&nbsp
                                            <option value="success">Completed</option>
                                            <option value="success">Approved</option>
                                            <option value="warning">Pending</option>
                                            <option value="danger">Not Approved</option>
                                            <option value="info">In Review</option></span></td>
                                                 <td>@(period.Status ? "Active" : "Inactive")</td>
                                                 <td>
                                                <a asp-controller="period" asp-action="edit" asp-route-id="@period.Id">Edit</a>|

                                               <a asp-controller="period" asp-action="delete" asp-route-id="@period.Id"
                                                      onclick="return confirm('Are you sure')">Delete</a>
                                                  </td>
                                              </tr>
                                         }
                                        </tbody>
                                    </table>
                                </div>
                                <!-- /.card-body -->
                            </div>
                            <!-- /.card -->
                        </div>
                    </div>


    <script>
        $(function () {  
            $('#example2').DataTable({
                "paging": true,
                "lengthChange": false,
                "searching": false,
                "ordering": true,
                "info": true,
                "autoWidth": false,
                "responsive": true,
            });
        });
    </script>


